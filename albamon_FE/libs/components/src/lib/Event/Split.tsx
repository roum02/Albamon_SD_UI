export const Split = ({ marginTop, marginBottom, marginLeft, marginRight }) => {
  return (
    <hr
      style={{
        marginTop,
        marginBottom,
        marginLeft,
        marginRight,
      }}
    />
  );
};
