import React from 'react';
import { EventTemplate, MainTemplate } from '@templates/lib';

const Home = () => {
  return (
    <div>
      <MainTemplate />
      <EventTemplate />
    </div>
  );
};

export default Home;
